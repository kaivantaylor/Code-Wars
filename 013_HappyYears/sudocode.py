some_func(a_year):
    if within values 1000 - > y -> 9000:
        I need to make the year into a seperated list
        i.e. [2,0,1,8]
        str -> str.split()?
        then once we have 2018
        remove duplicates..

        if len(that_list_we_just_did) == 4:
            continue loop
            return that number
        else:
            year + 1 and a_year(number)
